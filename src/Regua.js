import React from "react";

const Regua = (index, currentImage) => {
  return (
    <div>
      {console.log("index image " + { currentImage })}
      <div>Teste regua </div>
    </div>
  );
};

export default Regua;
